# ============================================
# Database Configuration
# ============================================
# SQLite database file path for PayloadCMS (legacy)
DATABASE_URI=file:./data/payload.db

# PostgreSQL Connection URL (recommended for production)
# Set DATABASE_URL or use individual PG* variables below
# To set DATABASE_URL, get the connection string from your database provider
# Common providers: Neon, Supabase, Railway, or self-hosted PostgreSQL

# Individual PostgreSQL connection settings
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=awesome_mac
# PGUSER=awesome_mac
# PGPASSWORD=YOUR_PASSWORD
# PGSSLMODE=require  # Options: disable, allow, prefer, require, verify-ca, verify-full

# Connection Pool Settings
# DB_POOL_MIN=2
# DB_POOL_MAX=10
# DB_IDLE_TIMEOUT=30000
# DB_CONNECTION_TIMEOUT=10000

# Migration Settings
# MIGRATION_OVERWRITE=false  # Set to 'true' to overwrite existing PostgreSQL data during migration
# SQLITE_DB_PATH=./data/payload.db  # Path to SQLite database for migration

# ============================================
# PayloadCMS Configuration
# ============================================
# Secret key for PayloadCMS encryption and signing
# Must be at least 32 characters long
# Generate with: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-key-here-min-32-chars-long

# ============================================
# API Authentication
# ============================================
# API key for protected sync endpoint
# Used to authenticate data synchronization requests
SYNC_API_KEY=your-sync-api-key-here

# ============================================
# GitHub Integration (Optional)
# ============================================
# Personal Access Token for GitHub API
# Provides higher rate limits for GitHub operations
# Create at: https://github.com/settings/tokens
# Required scopes: repo (public_repo), read:org
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# ============================================
# Google Analytics 4
# ============================================
# Google Analytics 4 Measurement ID
# Format: G-XXXXXXXXXX or G-A1B2C3D4E5
# Create at: https://analytics.google.com/
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Google Analytics 4 Measurement Protocol API Secret
# Used for server-side event tracking (Web Vitals, search analytics)
# Create at: https://analytics.google.com/ -> Admin -> Data Streams -> Choose stream -> Measurement Protocol
GA_MEASUREMENT_PROTOCOL_SECRET=your-measurement-protocol-secret

# ============================================
# Application URLs
# ============================================
# Public URL of the application (used for canonical URLs, OG tags, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Environment
# ============================================
# Node environment (development, production, test)
NODE_ENV=development

# ============================================
# SEO & Verification (Optional)
# ============================================
# Google Search Console verification code
# Add only the content value from: <meta name="google-site-verification" content="YOUR_CODE" />
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# ============================================
# Feature Flags
# ============================================
# Enable debug mode for analytics (shows events in console)
NEXT_PUBLIC_ANALYTICS_DEBUG=false

# ============================================
# Rate Limiting (Optional)
# ============================================
# Maximum requests per minute for API endpoints
RATE_LIMIT_MAX_REQUESTS=60

# Rate limit window in milliseconds
RATE_LIMIT_WINDOW_MS=60000

# ============================================
# Security Configuration
# ============================================
# CORS allowed origins (comma-separated)
# Example: http://localhost:3000,https://yourdomain.com,https://*.example.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://jaywcjlove.github.io

# Enable/disable HSTS preload (only for production with valid HTTPS)
SECURITY_HSTS_PRELOAD=true
